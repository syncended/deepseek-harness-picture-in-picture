/**
 * Host loader entry for the browser-only picture-in-picture plugin.
 * The actual implementation is exported from `./client` and discovered through
 * package.json's `dsh.client` metadata.
 */
export function apply() {}
